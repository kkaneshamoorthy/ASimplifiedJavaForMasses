write a function called main():
	define $height = get user input
	call printPattern($height)

write another function called printPattern($heightOfPattern):
	define $star = "*"
	define a loop to go over $heightOfPattern
		show $star
		$star = $star + "*"
	end
end
