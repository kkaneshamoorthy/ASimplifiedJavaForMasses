class Stack {
    stack : array;
    size : Int;
    top : Int;

    init() : Stack {

    }

    push(newData : Int) : void {
        top += 1;
        stack[top] = newData;
        size += 1;
    }

    pop() : Int {
        Int result = stack[top];
        top -= 1;
        size -= 1;

        return result;
    }
}